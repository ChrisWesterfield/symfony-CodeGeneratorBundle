    /**
     * @param $type
     * @param $message
     * @CG\Generated()
     *
     * @return void
     * @throws \LogicException
    */
    protected function addFlash($type, $message){% if strict %}:void{% endif %}
    {
        if(!method_exists($this,'getSession'))
        {
            throw new LogicException('You need to Include the Session Trait!');
        }
        /** @var SessionTrait $this */
        $this->getSession()->getFlashBag()->add($type,$message);
    }
