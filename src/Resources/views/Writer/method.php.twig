{% if methods|length > 0 %}{% for method in methods %}
    {% if method.comment|length > 0 %}{% for comment in method.comment %}
{{ comment|raw }}
{% endfor %}{% endif %}
    {% if method.final %}final {% endif %}{{ method.visibility }} function {{ method.name }}({% include 'MjrOneCodeGeneratorBundle:Writer:variables.php.twig' with {'variables': method.variables } %})
    {
{% if method.body|length > 0 %}{% for body in method.body %}
{{ body|raw }}
{% endfor %}{% endif %}
    }
{% endfor %}{% endif %}
